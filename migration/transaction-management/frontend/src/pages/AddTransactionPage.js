import React from 'react';
import Layout from '../components/Layout';

/**
 * CT02 — Add Transaction (COTRN02C). Wave A stub; account/card resolve, field +
 * date validation, max-key+1 key-gen, confirm and copy-last land in Wave D.
 */
export default function AddTransactionPage() {
  return (
    <Layout tranId="CT02" progName="COTRN02C" title="Add Transaction">
      <p>Add transaction (CT02) — coming in Wave D.</p>
    </Layout>
  );
}
