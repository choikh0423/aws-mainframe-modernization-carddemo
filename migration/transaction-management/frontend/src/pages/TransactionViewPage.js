import React from 'react';
import Layout from '../components/Layout';

/**
 * CT01 — Transaction View (COTRN01C). Wave A stub; the by-Tran-ID lookup and
 * read-only detail display land in Wave B.
 */
export default function TransactionViewPage() {
  return (
    <Layout tranId="CT01" progName="COTRN01C" title="View Transaction">
      <p>Transaction view (CT01) — coming in Wave B.</p>
    </Layout>
  );
}
